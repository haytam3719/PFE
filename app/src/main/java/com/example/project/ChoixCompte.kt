package com.example.project

/*
class ChoixCompte : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        //setContentView(R.layout.activity_choix_compte)
    }
}

/*
@Composable
fun FilledCardExample(width: Int, height:Int) {
    Card(
        colors = CardDefaults.cardColors(
            containerColor = MaterialTheme.colorScheme.surfaceVariant,
        ),
        modifier = Modifier
            .size(width = width.dp, height = height.dp)
    ) {
        Text(
            text = "Filled",
            modifier = Modifier
                .padding(16.dp),
            textAlign = TextAlign.Center,
        )
    }
}


@Preview
@Composable
fun ListCompte() {
    CenterAlignedTopAppBarExample(text = "Choix du type de compte")
    Column(
        modifier = Modifier.fillMaxSize(),
        verticalArrangement = Arrangement.Top,
        horizontalAlignment = Alignment.CenterHorizontally,
    ) {
        Text("Veuillez choisir le type de compte qui vous convient", Modifier.padding(100.dp))
    }
    Column(
        modifier = Modifier.fillMaxSize(),
        verticalArrangement = Arrangement.Center,
        horizontalAlignment = Alignment.CenterHorizontally,
    ) {
        Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.Center) {
            FilledCardExample(width = 150, height = 150)
            Spacer(Modifier.width(25.dp))
            FilledCardExample(width = 150, height = 150)
        }
        Spacer(Modifier.height(20.dp))
        Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.Center) {
            FilledCardExample(width = 150, height = 150)
            Spacer(Modifier.width(25.dp))
            FilledCardExample(width = 150, height = 150)
        }
        Spacer(Modifier.height(20.dp))
        Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.Center) {
            FilledCardExample(width = 150, height = 150)
        }

        }

    Column(
        modifier = Modifier.fillMaxSize(),
        verticalArrangement = Arrangement.Bottom,
        horizontalAlignment = Alignment.CenterHorizontally,
    ){
        Button(
            onClick = { /* Handle button click */ },
            modifier = Modifier.padding(50.dp)
        ) {
            Text("Continuer")
        }
    }
}

 */
*/